# Experiment

This repository is merely an experiment with letting GitHub Pages host a static website that is an HTML5/JavaScript/CSS
application.
In this case, it is a simple game that should run on any remote device with a modern browser and network connection.
